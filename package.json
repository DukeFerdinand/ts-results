{
  "name": "ts-results",
  "version": "0.1.0",
  "description": "A result wrapper based on the Rust `Result` enum",
  "main": "index.ts",
  "license": "MIT",
  "devDependencies": {
    "jest": "^26.6.3",
    "typescript": "^4.0.5"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc"
  }
}